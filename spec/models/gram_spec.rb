require 'rails_helper'

RSpec.describe Gram, type: :model do
  # it "should not create a gram without a message" do
  # 	# count = Gram.all.count
  # 	# FactoryBot.create(:gram)
  # 	# expect(Gram.all.count).to eq count
  # end


end
